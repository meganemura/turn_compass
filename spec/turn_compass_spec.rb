require 'spec_helper'

describe TurnCompass do
  it 'has a version number' do
    expect(TurnCompass::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
